---
project: My Dashboard
task: Build the shareable per-fork personal dashboard (productized "give-it-to-anyone")
slug: my-dashboard
effort: E4
phase: verify
progress: 69/80
mode: ALGORITHM
started: 2026-06-24
updated: 2026-06-30
---

# My Dashboard — Project ISA (system of record)

> Built from the FINAL adopted plan: `~/Plans/my-dashboard-build-plan.md` (my draft + Ultraplan, reviewed together).
> Repo home: `~/Projects/my-dashboard-app` (fresh build; the old clone-fork `~/Projects/my-dashboard` is set aside as reference).
> The project's higher-level system of record is `~/.claude/PAI/USER/PROJECTS/MY_DASHBOARD/PROJECT.md`; this ISA is the build's living spec.

## Problem

Yaron's dashboard (`my-jarvis-dashboard-yaron`) is a single-tenant personal tool. He wants a **productized, shareable** version: a thing he can hand to any person (5–50 hand-picked recipients) who then runs and adapts it as **their own per-person fork**, with physical data isolation, near-zero ongoing maintenance for the giver, and a genuinely non-technical handoff. The existing single-tenant data layer (no `owner_id`, queries return all rows) makes a multi-tenant SaaS (L3) a permanent cross-tenant-leak risk; the council chose **L2 physical isolation** (one D1 + one R2 + one Worker per recipient). No clean OSS drop-in exists, so the build is fresh on a Cloudflare-native stack — but ports proven modules from the existing repo rather than green-fielding.

## Vision

A non-technical person clicks one "Deploy to Cloudflare" button, authorizes once, and 60 seconds later has their **own** dashboard at their own URL — their own database, their own files, isolated by construction from everyone else's. It just works: every page shows real or gracefully-empty content, never a blank screen. The giver maintains the whole fleet from one re-runnable script. Euphoric surprise = "I gave this to someone and they made it theirs without me touching anything."

## Out of Scope

- Multi-tenant shared-query SaaS (L3) — explicitly rejected; isolation is physical, not by query filter.
- Refine / heavyweight CRUD framework — bare TanStack Query over Hono instead.
- Meetings / Vexa / Google-OAuth / per-minute calendar cron in v1 — deferred to P5 (per-fork secrets break the non-technical handoff).
- Memory page — parked (fed only by Yaron's PAI Stop hook; no recipient data source).
- Spend, Move, Rental, Situation, Agents, Skills pages — dropped from the productized scope.
- Instant fleet-wide sync — given up deliberately; the deploy loop is one command.
- Pages / `wrangler.toml` — Cloudflare steers new full-stack apps to Workers + `wrangler.jsonc`.

## Principles

- **Isolation by construction beats isolation by discipline.** A bug in one fork must be physically unable to reach another's data — no shared query path exists to forget a filter on.
- **Reliability → low giver-maintenance → non-technical handoff**, optimized in that order.
- **Port the proven, green-field only the seam.** The existing repo already runs the target stack; reuse `db.ts` unchanged, adapt only what the runtime forces (Pages-Function → Hono).
- **Trust only verified information** — every ISC closes on a tool-verified probe, never "should work."
- **Additive-only evolution after forks ship** — a config/schema change must never break a fork already in the wild.
- **Secrets stay server-side** — keys (`tools_key`, Anthropic key) never reach the browser.

## Constraints

- **Runtime:** one Cloudflare **Worker per fork** = React Router (SSR) + Hono `/api/*` + Static Assets, on `@cloudflare/vite-plugin`. Base = `cloudflare/react-router-hono-fullstack-template` (Hono 4.8.2, React 19.0.0, react-router 7.6.3, vite 6, tailwind 4, wrangler 4.21.x, ts 5.8.3).
- **Config = `wrangler.jsonc`** with binding placeholders + **IDs omitted** (deploy button auto-provisions and writes IDs back to JSONC only — workers-sdk #13632; TOML breaks `wrangler d1 migrations apply --remote`). Load-bearing for both deploy paths.
- **Per-fork bindings:** `DB` (D1), `BUCKET` (R2), `KV` (KV), `AI` (Workers AI). MCP write Durable Object added at P3.
- **Server dir is `workers/`** (template layout), not the plan's `/server/` — port target = `workers/lib/db.ts`, `workers/lib/auth.ts`.
- **Auth:** CF Access (humans) — verify the signed `Cf-Access-Jwt-Assertion` via team JWKS, **never trust a spoofable header**; scoped per-fork bearer for the agent/MCP seam.
- **Data layer:** TanStack Query → Hono `/api/*` → `getDb(env)` tagged-template over D1. No Refine.
- **Config:** single `settings` row (versioned JSON, Zod-validated, every field `.default()`, embedded `schemaVersion` + lazy `migrateConfig`).
- **Toolchain:** **bun** for install/scripts (never npm/npx). **Deploy must run under node**, not bun — `wrangler deploy` hangs mid-upload under bun on this Mac; use `/opt/homebrew/bin/node ./node_modules/wrangler/bin/wrangler.js deploy` wrapped in `script -q /dev/null`, no stdout redirect, no pipe. CF account ID stored locally (`~/.config/cloudflare/`), not committed — this repo is public.

## Goal

Ship a fresh Cloudflare-Workers dashboard that any hand-picked recipient can stand up as a physically-isolated per-fork instance via a one-click Deploy button, with versioned per-fork config, six v1 pages (Home, Goals, Projects, Portfolio, Tools, Knowledge Base), an MCP agent control plane (read tools + elicitation-gated writes), and a re-runnable fleet script — built by porting proven modules from `my-jarvis-dashboard-yaron` onto the `react-router-hono-fullstack-template` base. **Done when all ISCs below pass.** The current run targets **P0 (Foundation)**: prove per-fork auto-provisioned isolation + idempotent migrations before any page is ported.

## Criteria

> Numbered sequentially; never re-numbered on edit (splits become ISC-N.M). P0 is granular (active phase); P1–P5 are coarse placeholders to be split when each phase activates.

### Antecedent
- [ ] ISC-1: Antecedent: a recipient with no terminal/CLI access needs exactly one action — click the Deploy-to-Cloudflare button + authorize once — to get a running, isolated fork.

### P0 — Foundation (scaffold + isolation + deploy)  ✅ buildable surface VERIFIED (deploy-button proof deferred to Yaron)
- [x] ISC-2: `~/Projects/my-dashboard-app` is a fresh git repo with its own initial commit (`6759d59`; template `.git` removed; `git remote -v` empty).
- [x] ISC-3: Scaffold present from `cloudflare/react-router-hono-fullstack-template` — `app/`, `workers/app.ts`, `wrangler.jsonc`, `react-router.config.ts`, `vite.config.ts` all exist.
- [x] ISC-4: `bun install` completes (226 pkgs) and `bun.lock` exists; `package-lock.json` removed.
- [x] ISC-5: `wrangler.jsonc` declares a `DB` D1 binding with `database_name` set and `database_id` OMITTED.
- [x] ISC-6: `wrangler.jsonc` declares a `BUCKET` R2 binding with `bucket_name` set.
- [x] ISC-7: `wrangler.jsonc` declares a `KV` KV-namespace binding with `id` OMITTED.
- [x] ISC-8: `wrangler.jsonc` declares an `AI` Workers-AI binding.
- [x] ISC-9: `wrangler.jsonc` sets `compatibility_flags` including `"nodejs_compat"`.
- [x] ISC-10: Anti: no `database_id`, KV `id`, or other account-specific resource ID is committed in `wrangler.jsonc` (grep returns none).
- [x] ISC-11: `workers/lib/db.ts` ports `getDb(env)` with the identical tagged-template API and only requires `env.DB`.
- [x] ISC-12: `getDb` value normalization is preserved (undefined→null, boolean→0/1, Date→ISO, object/array→JSON).
- [x] ISC-13: `workers/lib/auth.ts` ports the CF Access JWT verifier — `jose.jwtVerify` validates issuer + **audience** + signature + exp; never trusts the raw header; exports `identifyAccessUser` + `isOwnerEmail` + `requireUser`.
- [x] ISC-14: A scoped-bearer verification function (`verifyBearer`, constant-time) exists for the MCP seam (stub for P0).
- [x] ISC-15: Migration runner applies numbered SQL from `db/migrations/` (bundled via `import.meta.glob`), keyed on binding `DB`, tracking applied names in `_migrations`.
- [x] ISC-16: `bun run migrate` twice reports "0 pending" the second time (run 1 applied 0001, run 2 applied none).
- [x] ISC-17: Boot-guard runs pending migrations on first `/api/*` request (proven: fresh-DB `/api/me` returned 401, not 500).
- [x] ISC-18: `bun run migrate` exists and applies pending migrations to a (local) D1 database.
- [x] ISC-19: `0001_init.sql` creates `_migrations(name TEXT PK, applied_at TEXT)` and `settings(id INTEGER PK CHECK(id=1), display_name, config, updated_at)` — both confirmed present in D1.
- [x] ISC-20: `/api/health` returns 200 without auth with a binding-presence map `{db,bucket,kv,ai}`; `/api/me` returns `{email,isOwner}` when authed.
- [x] ISC-21: Unauthenticated `/api/me` returns 401 (`{"error":"unauthorized"}`).
- [x] ISC-22: The app serves a live SSR landing at `/` rendering "My Dashboard" (200). NOTE: the dedicated `/home` *page key* + routing lands in P1; for P0 the live landing is `/`.
- [x] ISC-23: A TanStack Query provider + `app/lib/api.ts` fetch wrapper are wired. DONE in P1: `QueryClientProvider` in `app/root.tsx`; typed `apiGet/apiPut` + query hooks in `app/lib/api.ts`; every page reads through it.
- [x] ISC-24: `bun run typecheck` passes clean (after wrangler 4.104 bump — older wrangler rejected omitted IDs).
- [x] ISC-25: `bun run build` succeeds (after vite-plugin 1.42 bump; emits worker + client assets; migration `.sql` bundled).
- [x] ISC-26: `README.md` contains a "Deploy to Cloudflare" button (repo URL finalized at the remote step).
- [x] ISC-27: Anti: `/api/me` (and all non-health `/api/*`) pass the auth gate; `/api/health` is the sole documented public route (verified: health 200 public, me 401).
- [x] ISC-28: Anti: boot-time migration cannot double-apply — now guaranteed **by construction**: the DDL + completion row commit in one atomic `D1.batch()`, so a concurrent second isolate hits a PK conflict and rolls back entirely (advisor-driven redesign; sequential idempotency proven; a true multi-isolate race is not locally probeable).
- [x] ISC-29: Clicking "Deploy to Cloudflare" stands up a fork with its own freshly provisioned D1+R2+KV and reaches the live landing with zero manual glue. VERIFIED 2026-06-24: repo made public → one button click auto-provisioned D1 `my-dashboard-db` (uuid redacted), R2 `my-dashboard-bucket`, KV `My Dashboard`, new copy-repo, deployed Worker live on workers.dev (HTTP 200; `/api/health` reports db/bucket/kv/ai all true; Interceptor real-Chrome render confirmed). Reusable: the CF deploy-button React submit only fires on a TRUSTED Interceptor click with Chrome foregrounded.
- [x] ISC-30: A CF-Access request from an allow-listed email returns 200 on `/api/me` against a deployed fork. VERIFIED 2026-06-25 (agent=Rex): Access app `6e3245b6` + allow-policy (aitheroad@gmail.com, OTP IdP) on `my-dashboard.aitheroad.workers.dev` — **Access enforces directly on workers.dev** (302→`small-fire-f8d3.cloudflareaccess.com`; no custom domain needed). Authed real-Chrome → `/api/me` 200 `{"email":"aitheroad@gmail.com","isOwner":true,"mode":"access"}` (mode:access = verified-JWT path). Unauth → 302. Current code (Settings/Tools/KB) live; migrations self-healed (D1 `_migrations`=[0001,0002,0003]).

### P1 — Easy pages + customization + first-run
> ISC-31 split into per-page probes (ID-stability: parent preserved as this header). "Slice 1" = the spine + 4 pages live with empty states + viewer auth; Settings UI export/import (ISC-33) + full toggle round-trip (ISC-34) + migration fixtures (ISC-36/37 tests) follow.
- [ ] ISC-31: [SPLIT — see ISC-31.1..31.5]
- [x] ISC-31.1: `/api/projects` (Hono, `getDb`) returns projects rows; `/projects` page renders them (or styled empty state). Verified: curl 200 → 2 demo projects w/ goal_count subquery; real-Chrome prod render shows 2 cards.
- [x] ISC-31.2: `/api/goals` (Hono, `getDb`, project join) returns goals rows; `/goals` page renders them (or empty state). Verified: curl 200 → 3 goals w/ project_name join; real-Chrome render shows 3 with project labels.
- [x] ISC-31.3: `/api/portfolio` returns an empty productized snapshot (Yaron's personal holdings NOT shipped); `/portfolio` page renders the "connect your portfolio" empty state. Verified: curl `configured:false, holdings:[]`; real-Chrome shows "No portfolio connected".
- [x] ISC-31.4: `/` Home renders an overview (project/goal counts + nav) from live `/api/*`, never blank. Verified real-Chrome: "2 active projects / 2 active goals", welcome card, recent-projects list.
- [x] ISC-31.5: A `getViewer(request, env)` seam gates data endpoints: enforces verified CF Access when the fork configures it, allows owner open-dev when Access is unconfigured (local dev / bare fork). Anti: an Access-configured fork still 401s an unverified request. Verified via probe: access-env + no token → null (401); bare env → open-dev owner.
- [x] ISC-32: Zod `ConfigSchema` with embedded `schemaVersion`, every field `.default()`, and a lazy `migrateConfig(raw)` chain. Verified: `/api/settings` returns fully default-filled config; probe confirmed defaults.
- [x] ISC-33: Settings UI reads/writes the `settings` row; export = validated blob, import = validate + migrate-to-current. DONE (Block A 2026-06-25): owner-only `/settings` page (display_name/theme/page-toggle/reorder) + Export (download config JSON) + Import (file→parse→PUT, server mergeConfig validates+migrates). Real-Chrome verified render + toggle→Save persist; import of `{schemaVersion:0,theme:"dark"}` → migrated to v1 theme kept; garbage-typed import → HTTP 200 no throw.
- [x] ISC-34: Page manifest filtered by `enabled_pages` + ordered by `page_order`; toggling a page off removes it from sidebar + routes next load and survives export→fresh-fork→import. DONE (Block A 2026-06-25): toggle Goals off + Save → sidebar drops Goals (real-Chrome: links = /,/projects,/portfolio,/settings) AND `/goals` redirects to `/` (useRequireEnabled). Reorder via page_order PUT → resolved order changes. Export blob = settings.config verbatim → re-import via PUT keeps it. `navFromPages` restricted to BUILT_PAGES (no 404 nav links).
- [x] ISC-35: First-run shows seeded demo content or a styled empty state on every page — never blank. Verified: demo seed renders on Home/Projects/Goals; Portfolio + each list have styled EmptyState components.
- [x] ISC-36: Importing an OLD exported config into a newer schema yields a valid merged config (defaults fill gaps), no error. Verified via probe: `{schemaVersion:0, theme:"dark", ...}` → schemaVersion 1, theme kept, defaults filled.
- [x] ISC-37: Anti: an additive new config key never breaks import of a config exported by an older fork. Verified via probe: unknown page key "FUTURE_PAGE" dropped (not fatal); null/string/bad-type inputs never throw (`.catch()` per field).

### P2 — Tools embed + Knowledge Base
- [x] ISC-38: Tools page embeds pai-tools via a per-fork `tools_key`; absent key → styled "not configured" state. DONE (Block B 2026-06-25): no key → "Tools not configured" + Open Settings link; key set → page renders the embedded toolset (flux/tts/ocr/whisper catalog via `/api/tools/status`) + connected/rejected banner. NOTE: the green "valid ✓" badge needs a LIVE valid pt_ key (the local test key is revoked → pai-tools 401); the embed itself (toolset render) + injection are proven. Green-badge live-flip = [DEFERRED-VERIFY] (follow-up: provide a fresh pt_ key).
- [x] ISC-39: Anti: `tools_key` never reaches the browser — tool calls proxy through `/api/tools/*` server-side. DONE: `settingsView()` redacts `tools_key`→null on every GET/PUT /settings response (+ `tools_configured` bool); the key VALUE is ABSENT from `build/client` (grep); the proxy injects `Authorization: Bearer <key>` server-side — PROVEN because pai-tools returned "key not recognized" (i.e. the key WAS forwarded), not "missing Authorization". (The identifier string "tools_key" appears in settings.js only as the send-only PUT field name — not the secret.)
- [x] ISC-40: KB data-driven index (`/api/kb`) + `/kb-doc/*` detail + BlockRenderer (13 section types) renders without error. DONE: `/api/kb` lists 3 docs; `/kb-doc/blocks-reference` real-Chrome render = h1+h2, 4 lists, table, code pre, blockquote, hr, image figure, 2 external links — all 13 distinct block types render, no failure. Generic XSS-safe renderer (plain-text children).
- [x] ISC-41: A small generic starter KB doc set is seeded (no MyJarvis-specific docs). DONE: `0003_p2.sql` seeds welcome / customize / blocks-reference (generic dashboard docs; check:clean passes 33 files).

### P3 — Agent layer (one control plane, two clients)
- [x] ISC-42: Read-only MCP tools via stateless `createMcpHandler()` at `/mcp` with reused Zod schemas; scoped-bearer auth; invalid bearer rejected. DONE (Block C Slice 0 2026-06-25): `workers/mcp.ts` exposes 6 read tools (list_projects/list_goals/get_portfolio/list_kb/get_kb_doc/get_settings) via `createMcpHandler`; mounted at `/mcp` in app.ts behind `verifyBearer`. Verified (dev server, loads `.dev.vars`): no/wrong bearer → 401; valid bearer → initialize 200 + `tools/list` (6 tools) + `tools/call list_projects` → live data. 401 body never echoes the token; 0 write tools registered. NOTE: transport may change under the McpAgent DO (Slice 1) — the read mount is not a frozen contract.
- [x] ISC-43: Guarded KB write with confirm gate — declining (confirm≠true) aborts with no DB change; accepting writes + inserts exactly one `mcp_activity` row, atomically. DONE 2026-06-26 (Block C Slice 1): `add_kb_doc`/`edit_kb_doc` in `workers/mcp.ts` write only on `confirm:true` (the ISA's sanctioned fallback); store.ts commits the doc change + one audit row in a single `env.DB.batch()`. **Refined from the planned McpAgent DO** (interactive elicitation needs a stateful transport AND an elicitation-capable client — unverifiable here — and a DO migration touches the deploy-button wrangler.jsonc; the confirm-gate satisfies the verifiable contract with zero deploy-config risk). VERIFIED via a real MCP SDK client against live D1: confirm:false → `confirmation_required`, 0 audit rows; confirm:true → created; edit → updated; 2 writes → exactly 2 atomic audit rows; duplicate/missing → isError, 0 rows. Test data cleaned.
- [x] ISC-44: Built-in Assistant pane answers a read question on `env.AI`; opt-in Anthropic key via AI Gateway. DONE 2026-06-26 (Block C Slice 2): `/api/assistant` (owner-gated, `requireViewer`+mode:access+isOwner) → `workers/services/assistant.ts` grounds a system prompt in live project/goal/KB context and runs `env.AI` (default `@cf/meta/llama-3.1-8b-instruct-fp8`); opt-in Anthropic via AI Gateway when `ANTHROPIC_API_KEY`+`AI_GATEWAY_BASE_URL` set (key stays server-side). New `/assistant` page (sidebar + manifest + route). VERIFIED real-Chrome (authed): asked "What projects and goals do I have?" → correct grounded answer naming the 2 projects + 3 goals, tagged `workers-ai · @cf/meta/llama-3.1-8b-instruct-fp8`.
- [x] ISC-45: MCP tools call shared service functions, not internal HTTP round-trips. DONE (Block C Slice 0): `workers/services/store.ts` is the single data home; both the Hono `/api/*` handlers and the MCP tools call it (no internal HTTP). HTTP routes regression-verified post-refactor (projects/goals/kb/settings all 200).
- [x] ISC-46: Anti: no write tool executes without auth → server-side Zod validation → audit row. DONE 2026-06-26 (Block C Slice 1): /mcp gates on `verifyBearer` (app.ts) before any tool runs; write tools have Zod `inputSchema` (slug/title/blocks/confirm) + a server-side `isValidSlug`/title guard; every successful write commits its audit row in the same atomic batch. VERIFIED: invalid slug → isError (no write/no row); no-bearer /mcp → 401; declined write → 0 rows.

### P4 — Fleet + first real fork
- [ ] ISC-47: `recipients.json` manifest (name, email, subdomain, `enabled_pages` overrides) is the single source of truth.
- [ ] ISC-48: `deploy-fleet.mjs` is idempotent — running it twice over the same manifest is a no-op the second time.
- [ ] ISC-49: `smoke.mjs` per-fork health check is green (home loads, `/api/me` 200, migrations current, seed present).
- [ ] ISC-50: One real recipient logs in via CF Access, sees their seeded dashboard, edits a page — zero manual steps after the script run.

### P5 — Meetings module (deferred)
- [ ] ISC-51: Meetings module ported verbatim, ships `enabled:false` in default config; a fork that leaves it disabled is entirely unaffected.
- [ ] ISC-52: A fork that enables Meetings + supplies secrets gets a bot joining a test call + a rendered transcript.

### Cross-cutting
- [ ] ISC-53: Anti: no recipient fork can read or write another fork's D1/R2 (no shared binding, no cross-fork query path exists).

### Tools Workspace (functional home) — built 2026-06-29
> Promotes the Tools page from catalog-embed (ISC-38) to a functional workspace where the fork's tools are actually USED. FirstPrinciples UX: zero setup, one calm sectioned surface, input→visible output in place, honest waiting/failure, gallery memory, graceful gate. Access model = inside the gated fork (Yaron's pick 2026-06-29).
> **NATIVE REBUILD 2026-06-29 (decision: "build them natively"):** the pai-tools proxy + per-fork `pt_` key were REMOVED. The four tools now run on THIS fork's own `env.AI` (Workers AI) — image/STT/OCR fully native; TTS via an optional server-side OpenAI key (multilingual) with `@cf/myshell-ai/melotts` as the English fallback. Generated media lives in the fork's own KV, served from a same-origin owner-gated route. This kills CF error 1042 at the root (no worker→worker hop), removes the key from the system entirely, and gives correct per-fork billing. Gate is unchanged: the dashboard's own CF Access (the caller is already signed in). The contracts (response shapes) were deliberately preserved so the panels are unchanged. New core: `workers/services/tools.ts`. (pai-tools stays alive for its other clients — Noa's connector/MCP/Telegram; the dashboard just stops depending on it.)
- [x] ISC-54: Tools page renders a sectioned workspace (Image · Speak→Text · Text→Speech · Read Text · Gallery). **NATIVE UX (no key gate):** renders the workspace for the signed-in CF-Access owner (`ready=canUse`); non-owner/open-dev → graceful "Sign in (Cloudflare Access)" card; a TTS-language banner (amber without an OpenAI key, green with). RE-VERIFIED LIVE 2026-06-29 on the native deploy (version `5c4b2bb8`, Interceptor authed Chrome): /tools renders the 5 section tabs + Image panel + the amber "add an OpenAI key" banner (correct — no key set) as the authed owner. (The earlier "key-configured / key-rejected amber banner" wording + version `1911ed97` evidence described the SUPERSEDED proxy/key-gate build — corrected here per Cato's fidelity finding.)
- [x] ISC-55: Image: prompt (+high/fast) → POST /api/tools/flux → inline `<img>` (data:image/jpeg) + Download. **VERIFIED LIVE 2026-06-29** (version `5c4b2bb8`, authed Chrome): typed a prompt → High quality → Generate → native `/api/tools/flux` returned 200 (**NO 1042**), the panel rendered the image + a Download link with `href=data:image/jpeg;base64,/9j/…` (JPEG). End-to-end native image generation proven.
- [x] ISC-56: Text→Speech: text (+voice) → /api/tools/tts → inline `<audio>`(audio_file) + open/download. ✅ VERIFIED LIVE 2026-06-30 (Interceptor authed Chrome): English text → Speak → `/api/tools/tts` 200 `{engine:"deepgram:aura-1",chars:52}`; `<audio controls>` rendered + "Open / download audio" link; media route → `audio/mpeg` 200.
- [x] ISC-57: Speak→Text: audio upload → /api/tools/whisper → transcript + Copy + detected language. ✅ VERIFIED LIVE 2026-06-30: injected a 16 kHz m4a ("dashboard whisper test one two three") → `/api/tools/whisper` 200 `{text:"Dashboard Whisper Test 123.",word_count:4,language:"en"}`, rendered "Detected: en · 4 words" — strong fidelity. (Live MIC capture not exercised; upload path proven.)
- [x] ISC-58: Read Text: photo → base64 → /api/tools/ocr → extracted text + preview + Copy. ✅ VERIFIED LIVE 2026-06-30 on **Mistral Small 3.1** (`@cf/mistralai/mistral-small-3.1-24b-instruct`, swapped from llava): same PNG → `/api/tools/ocr` 200 `{text:"Invoice 4071 EUR 128.50"}` — EXACT (llava had garbled to "4007171 EURO"). Fidelity gap RESOLVED. Staging v`7999c261`.
- [x] ISC-59: Gallery: owner-gated media/list + voice/list + text/list → same-origin enriched items; renders image grid + clip players + transcripts. ✅ VERIFIED LIVE 2026-06-30: all three list endpoints fired; sections "Images" (1 img), "Voice clips · kept 14 days" (14 `<audio>` players), "Transcripts" all rendered.
- [x] ISC-60: Anti: the per-fork tools_key never reaches the browser via any new path — panels call same-origin /api/tools/* only. VERIFIED 2026-06-29: key absent from `build/client` (grep), no `pt_` literal in bundle. SUPERSEDED by the native rebuild — the `pt_` key/proxy are gone; the deprecated nullable `tools_key` field + its derived `tools_configured` boolean still emit to clients but are DEAD (no consumer in the native UI — cleanup follow-up, per Cato 3b). The live secret is now `openai_key`, covered by ISC-66.
- [ ] ISC-61: Anti: tool-run + gallery routes refuse unauth (401) + non-owner/open-dev (403); spend gate (mode:access+isOwner) unchanged. CODE PRESENT (getViewer→401, `mode!=="access" || !isOwner`→403 on POST /tools/:tool, both gallery lists, AND the media-serve route); live probe [DEFERRED-VERIFY → ISC-62].
- [x] ISC-62: Antecedent: on a CF-Access fork, a real end-to-end tool call completes NATIVELY (this fork's `env.AI`) and renders inline — no proxy, no worker→worker hop, no 1042. **PASSED LIVE 2026-06-29** (version `5c4b2bb8`, Interceptor authed Chrome): a real image generation rendered inline (JPEG `/9j/` data URL + Download); `/api/tools/flux` returned 200, NOT 1042. The native rebuild removed the blocker at its root (was BLOCKED by CF error 1042 under the pai-tools proxy design). Upstream also proven: auth `mode:access` (ISC-30), owner spend gate enforced. **Scope honesty:** only the IMAGE path is live-exercised this session; whisper/ocr/tts run the same native pattern + are code-complete but NOT yet live-verified (see ISC-56..58, ISC-63).
- [x] ISC-63: Native execution — /api/tools/{flux,whisper,ocr,tts} run on this fork's `env.AI` (flux-schnell/lucid-origin · whisper-large-v3-turbo · llava-1.5-7b · deepgram:aura-1), no pai-tools, no `pt_`, no worker→worker. ✅ VERIFIED LIVE 2026-06-30: flux (ISC-55), whisper (ISC-57), ocr (ISC-58), tts (ISC-56) all returned 200 natively — NO 1042 on any.
- [x] ISC-64: TTS language — English → `@cf/deepgram/aura-1` (keyless, 12 voices); **Hebrew → Microsoft Edge** (keyless, Avri/Hila) via `edge-tts.ts`; OpenAI branch dormant (not wired, never reached for Hebrew). ✅ SUPERSEDED + RESOLVED 2026-06-30 by ISC-73/74/75 — Hebrew is now keyless via Edge; the earlier "Hebrew needs an OpenAI key" is no longer true. (Earlier same-day finding that no-key Hebrew silently fell back to English Aura-1 was BEFORE the Edge wiring.)
- [x] ISC-65: Generated media persists in the fork's OWN KV + served by owner-gated `GET /api/tools/media/:kind/:id` (id validation + kind allowlist + content-type). ✅ VERIFIED LIVE 2026-06-30: TTS clip stored + served `audio/mpeg` 200 from `/api/tools/media/audio/<uuid>`; 14 clips persist in Gallery across sessions.
- [x] ISC-66: Anti: `openai_key` never reaches the browser — `publicSettings()` strips it, returns only booleans. ✅ VERIFIED 2026-06-30: grep of fresh `build/client` → no `sk-` key (only Tailwind `sk-image-*` mask tokens), no `pt_`; `openai_key` appears only as `openai_key:null` (redacted) + the write-only form field. No secret value in bundle.
- [ ] ISC-67: Anti: a bare open-dev fork (no CF Access) cannot spend the fork's AI quota or read its media — every native tool-run, gallery-list, and media-serve route requires `viewer.mode==="access" && viewer.isOwner` (refuses open-dev with 403). CODE PRESENT; live open-dev probe [DEFERRED-VERIFY].

### Tools — Yaron live-feedback round (2026-06-29)
- [x] ISC-68: Speak→Text transcripts persist — each transcription auto-saves to `TEXT_INDEX` (cap 100); owner-gated `GET /api/tools/text/list` (`listTranscripts`); Gallery has a Transcripts section (copy + delete). VERIFIED LIVE (version `49c51d65`): endpoint returns `{items:[]}` 200; section renders. [Full mic→transcript→save loop pending a real recording.]
- [x] ISC-69: Gallery items are deletable — owner-gated `DELETE /api/tools/media/:kind/:id` (blob + index, `deleteMedia`) + `DELETE /api/tools/text/:id` (`deleteTranscript`); per-item trash button on images/clips/transcripts. VERIFIED LIVE: deleted the duplicate image → Images section went 2→1.
- [x] ISC-70: TTS voice picker is honest — gated on `tts_multilingual`; with no OpenAI key the 11-voice `<select>` is replaced by "One English voice. Add an OpenAI key for 11 voices + other languages." VERIFIED LIVE (`tts_multilingual:false` → hint shown). (Real multi-voice still requires pasting an OpenAI key.)
- [x] ISC-71: Image generation can't double-submit — synchronous `useRef` re-entrancy guard in `ImagePanel` (the `disabled`/`isPending` flags update only next-render, so they can't block a same-tick second click — the cause of the two identical images). CODE COMPLETE + duplicate cleaned via ISC-69.
- [x] ISC-72: No-key TTS offers multiple voices natively — `@cf/deepgram/aura-1` (12 English voices) replaces melotts as the no-key engine; `toolsStatus.tts_voices` drives a data-driven picker (12 Aura voices w/o key, 11 OpenAI voices w/ key); OpenAI gpt-4o-mini-tts stays the Hebrew/multilingual upgrade. VERIFIED LIVE (version `8147f8d6`): status `tts_engine:"deepgram:aura-1"`; generated a clip with `speaker:zeus` → audio rendered + saved (`engine:"deepgram:aura-1"`). No CF TTS model supports Hebrew (verified) → that gap is OpenAI-only. **[SUPERSEDED 2026-06-30: Hebrew gap closed keylessly via Microsoft Edge — see ISC-73..75 below.]**

### Hebrew TTS via Microsoft Edge (keyless) — 2026-06-30
> Yaron's call: Edge = the ONLY Hebrew engine, male/female selectable, no OpenAI key (deferred). Protocol implemented from scratch in TS (proven first in a bun probe), ported to workerd. New module `workers/services/edge-tts.ts` + shared `workers/services/tool-error.ts`.
- [x] ISC-73: `synthesizeHebrew(text, voice)` runs the MS Edge read-aloud WS protocol on workerd — fetch-upgrade WebSocket (**https:// scheme**, not wss://) + `crypto.subtle` SHA-256 GEC token (BigInt ticks) + speech.config/SSML frames + big-endian frame parse + turn.end. ✅ VERIFIED LIVE 2026-06-30 (staging v`7999c261`): Hila → 200 `engine:microsoft-edge:he-IL-HilaNeural`, media route serves 44784 bytes audio/mpeg; Avri → 200. **workerd CAN reach Microsoft.**
- [x] ISC-74: TTS panel exposes a Language selector (English / עברית) + per-language voice picker; Hebrew shows "Avri (male)" / "Hila (female)". ✅ VERIFIED LIVE (full UI loop, Interceptor authed Chrome): select עברית → voices swap to Avri/Hila → type Hebrew → Speak → 200 + `<audio>` rendered in panel.
- [x] ISC-75: Anti: Hebrew never routes to OpenAI and needs no key — `synthesize()` routes any Hebrew Edge voice to `synthesizeHebrew` BEFORE the dormant OpenAI branch. ✅ VERIFIED: engine = `microsoft-edge:*` with NO key set; English stays on Aura-1 (unchanged).

## Test Strategy

| isc | type | check | threshold | tool |
|-----|------|-------|-----------|------|
| ISC-2 | build | fresh git history, no template remote | initial commit only | `git log`, `git remote -v` |
| ISC-3 | build | scaffold files present | all listed exist | `ls` |
| ISC-4 | build | bun.lock present, package-lock.json gone | both true | `ls` |
| ISC-5–9 | config | bindings + flags present, IDs omitted | exact | `Read` wrangler.jsonc + grep |
| ISC-10,27,28 | security | no committed IDs; no unauthed /api; no double-apply | 0 violations | grep + curl + synthetic concurrent probe |
| ISC-11,12 | functional | getDb API + normalization | identical to source | `Read` + unit probe |
| ISC-13,14 | auth | JWKS verify, exports present, bearer fn | present | `Read` + grep |
| ISC-15–19 | data | migrations apply, idempotent, schema landed | "0 pending" 2nd run; tables exist | `bun run migrate` ×2 + D1 SELECT |
| ISC-20,21,22 | api/ui | health 200, /api/me 401 unauth, /home renders | exact status | `curl -i` local dev + Interceptor |
| ISC-23,24,25 | build | provider wired, typecheck + build green | exit 0 | `Read` + `bun run typecheck/build` |
| ISC-26 | build | deploy button markup in README | present | `Read` |
| ISC-29,30 | deploy | 2nd fork via button, CF-Access 200 | live | deploy button + Interceptor (DEFERRED) |
| P1–P5 | — | split when phase activates | — | — |

## Features

| name | satisfies | depends_on | parallelizable |
|------|-----------|-----------|----------------|
| F0.1 Fresh repo + bun toolchain | ISC-2,3,4 | — | no (gates all) |
| F0.2 wrangler.jsonc bindings | ISC-5,6,7,8,9,10 | F0.1 | yes |
| F0.3 Port db.ts | ISC-11,12 | F0.1 | yes |
| F0.4 Port auth.ts + bearer | ISC-13,14 | F0.1 | yes |
| F0.5 Migration runner + 0001_init | ISC-15,16,17,18,19,28 | F0.3 | no |
| F0.6 Hono /api wiring + auth gate | ISC-20,21,27 | F0.3,F0.4 | no |
| F0.7 TanStack provider + api.ts + /home | ISC-22,23 | F0.1 | yes |
| F0.8 typecheck + build green | ISC-24,25 | all above | no |
| F0.9 README deploy button + commit | ISC-26 | F0.8 | no |
| F0.10 Deploy-button 2nd-fork proof | ISC-1,29,30 | F0.9 + GitHub remote + OAuth | DEFERRED (Yaron) |

## Decisions

- 2026-06-29 (Tools — Deepgram Aura-1 wired as no-key TTS engine, ISC-72): Yaron's call after reviewing the live CF TTS catalog. **Checked all 4 Cloudflare Workers AI TTS models live** (account `a28d6c97…` + CF docs): `@cf/myshell-ai/melotts` (1 voice/lang, no Hebrew, ~free $0.000205/audio-min), `@cf/deepgram/aura-1` (12 EN voices, $0.015/1k chars), `@cf/deepgram/aura-2-en` (40 EN voices, $0.03), `@cf/deepgram/aura-2-es` (ES). **Key finding: NO Cloudflare TTS model does Hebrew** → Hebrew stays on OpenAI gpt-4o-mini-tts (or future self-host). Wired **Aura-1** as the no-key engine (replaces melotts): `synthesize` no-key branch → `env.AI.run("@cf/deepgram/aura-1", {text, speaker, encoding:"mp3"}, {returnRawResponse:true})` → reads the streamed Response → bytes → KV (verified the binding returns a stream/Response of MPEG via CF docs before coding). `runAI` extended to pass the binding's 3rd options arg. `toolsStatus` now returns `tts_engine` + `tts_voices` (active engine's list) so the picker is data-driven; the 12 Aura voices show without a key (default asteria), the 11 OpenAI voices show with a key. **VERIFIED LIVE** (version `8147f8d6`, Interceptor authed Chrome): status → `tts_engine:"deepgram:aura-1"` + 12 voices; picker shows them; typed text + selected `zeus` + Speak → real audio rendered + saved to gallery with `engine:"deepgram:aura-1"` (older clips show `melotts`, confirming the switch). tsc+build+check:clean green; commit `a2924bd`. [Yaron to confirm by ear that the voices sound distinct; Hebrew still needs the OpenAI key.]
- 2026-06-29 (Tools — Yaron live-feedback round, ISC-68..70): Yaron tested the deployed tools and gave 3 findings, all addressed + verified live (version `49c51d65`). (1) **Two identical images** in the gallery — root cause = a double-submit (gallery showed 2, same prompt, 661ms apart; my single automated Generate click double-fired before `isPending` re-rendered). Fixed with a synchronous `useRef` re-entrancy guard in `ImagePanel` (the `disabled`/`isPending` flags only update next-render, so they can't stop a same-tick second click). Added a per-item **Delete** (trash) across images/clips/transcripts (new owner-gated `DELETE /api/tools/media/:kind/:id` + `/tools/text/:id`; `deleteMedia`/`deleteTranscript` in tools.ts) → deleted the duplicate live (gallery → 1 image). (2) **Speak→Text transcripts not kept** — the transcript already renders after stop (with Copy), but was transient; now `transcribe` auto-saves each transcript to a new `TEXT_INDEX` (`listTranscripts` + `GET /api/tools/text/list`, owner-gated) and the Gallery has a **Transcripts** section (copy + delete). Verified endpoint live (`{items:[]}`). (3) **TTS "only one voice"** — root cause = the 11 voices are OpenAI voices, only active with a key; with no key the melotts fallback (one English voice) ignores the picker. Made it honest: the voice `<select>` is gated on `tts_multilingual` — no key → "One English voice. Add an OpenAI key for 11 voices + other languages." (verified live: `tts_multilingual:false` → hint shown, label "Text to speak (English)"). **The real multi-voice + Hebrew fix is still pasting an OpenAI key** (handoff file `OPENAI_API_KEY.LOCAL.txt`). tsc+build+check:clean green; commit `451cd99`. **Not yet live-exercised:** the full mic-record → transcript → auto-save-to-gallery loop (needs real audio; pieces verified).
- 2026-06-29 (Native rebuild DEPLOYED + image gen VERIFIED LIVE — ISC-62 closed): Committed (`72d343e`) + deployed the native rebuild to the gated fork (version `5c4b2bb8`; bindings env.AI/KV/DB/BUCKET present; new `tools-D-suwK40.js` chunk). Access config confirmed set as **secrets** pre-deploy (no auth regression — the deploy did NOT drop the fork to open-dev this time). **Interceptor live verify (authed Chrome):** /tools renders the workspace as the signed-in owner (the `ready=canUse` path, not the open-dev sign-in card); typed a prompt → High quality → Generate → image rendered inline with a JPEG (`/9j/`) data-URL Download link → **`/api/tools/flux` 200, NO CF 1042**. ISC-54 re-verified, ISC-55 + ISC-62 PASSED. **Cato (cross-vendor ISA audit) — verdict `skipped`:** the GPT-5.5 codex pass TIMED OUT (bundle ~37k tokens / 149k chars; no JSON) → the cross-vendor ISA independence was NOT obtained this run. (Forge's code-level GPT-5.5 cross-vendor pass DID land earlier + was clean — that's the security-critical one.) Cato's supplementary same-family (Opus) read = `concerns/medium`, no critical/high, gating + redaction sound. Its 3 legit findings folded: (a) ISC-54 fidelity gap — its text/evidence described the superseded key-gate UX → rewritten to the native CF-Access-owner UX + re-verified live; (b) **3b** dead `tools_configured`/`tools_key` still emitted (ISC-60 wording corrected; code cleanup deferred — harmless, migration-safe to leave); (c) **3c** the LOW-4 seed-string fix reaches FRESH forks only — the custom migration runner name-tracks `_migrations`, so `0003_p2.sql` never re-runs on already-migrated forks (incl. Yaron's live fork), which keep the old "needs pai-tools key" seed rows (fix = a `0005` migration or a one-off D1 UPDATE — deferred, KB doc is user-editable). Also trimmed the OpenAI-upstream error body out of the client-facing TTS 502 (info hygiene; ships with the next deploy). **Re-run Cato cross-vendor with a trimmed bundle if true ISA cross-vendor coverage is needed before the remaining tools (whisper/ocr/tts) are claimed.**
- 2026-06-29 (Tools NATIVE REBUILD — "build them natively", ISC-62..67): Removed the pai-tools proxy + per-fork `pt_` key entirely. The four tools now run on the fork's OWN bindings: image (`@cf/leonardo/lucid-origin` high / `@cf/black-forest-labs/flux-1-schnell` fast), STT (`@cf/openai/whisper-large-v3-turbo`), OCR (`@cf/llava-hf/llava-1.5-7b-hf`) all on `env.AI`; TTS via OpenAI `gpt-4o-mini-tts` when a key is set (multilingual incl. Hebrew) else `@cf/myshell-ai/melotts` (English WAV). Generated media persists in the fork's own KV (`tools:img:*` no-TTL, `tools:audio:*` 14d) and is served same-origin by owner-gated `GET /api/tools/media/:kind/:id`. **Why (FirstPrinciples):** the proxy's worker→worker hop was the *only* cause of CF error 1042 (which blocked ISC-62) AND the only reason a key existed in the dashboard at all — removing it kills the blocker at the root, deletes the secret, and bills each fork's own account. The caller is already CF-Access-authenticated, so the dashboard's own gate is sufficient; no second credential is justified. **Contracts preserved** (response shapes unchanged) → the panels (`tools-panels.tsx`) needed zero changes. New core file `workers/services/tools.ts`; `data.ts` tool routes rewired proxy→native dispatch + media-serve; `config.ts`/`env.ts`/`store.ts` gained the optional `openai_key` (server-side, redacted) + `OPENAI_API_KEY` fallback; `settings.tsx` key card repurposed to an optional OpenAI-for-TTS field; `tools.tsx` always renders the workspace (no key gate). Typecheck + build + check:clean all green. pai-tools left running for its other clients (Noa connector / MCP / Telegram). **Forge code audit DONE (Opus + GPT-5.5 codex, cross-vendor — no divergence): NO critical, NO high.** Secret-leak path (redaction holds through all 6 settings-returning routes), auth gating (all 4 native routes enforce `mode==="access" && isOwner`), media-serve safety (isMediaId charset blocks traversal/injection), and 1042-leftover sweep all verified clean. 4 MED + 5 LOW (completeness, not security) — **all fixed:** MED-1 AI-call timeouts (`runAI` 60s race→504); MED-2 orphaned image blobs (pushIndex now deletes evicted blobs when the 100-cap slices them off); MED-3 invalid client base64 →400 (`decodeB64Input`) + non-ToolError catch sanitized to 502 (no internal-message leak); MED-4 `toolsStatus` now reflects viewer posture (`ready=canUse`) so an open-dev/non-owner fork shows an honest "sign in" state instead of a green banner over 403s; LOW-1 ID_RE tightened to strict UUID-v4; LOW-3 stale proxy JSDoc rewritten; LOW-4 seed-doc "needs pai-tools key" strings replaced; LOW-5 `x-content-type-options:nosniff` on media. Re-green after fixes (tsc 0, build ok, check:clean). Remaining: Cato ISA audit + advisor boundary call → then deploy + Interceptor live image-gen (closes ISC-62).
- 2026-06-29 (Tools workspace built + deployed; two operational findings): Built the functional Tools workspace (ISC-54..62) — native panels over the server-side proxy, FirstPrinciples UX, access = inside the gated fork (Yaron's pick). Forge (GPT-5.5, no critical/no key-leak) + advisor folded in before commit `8985085`: **H1** gallery open-dev leak (tightened both gallery routes to `mode==="access"` parity with spend), **H2** recorder mic-leak on unmount (full lifecycle cleanup + 2-min cap), **M1–M6/L2/L3** (redaction parity, id guard, 502-on-upstream-error, error states, OfflineAudioContext, https URL guard). Deployed to the gated fork (version `1911ed97`); ISC-54 verified live. **Finding 1 — deploy wiped per-fork Access vars:** `wrangler deploy` from the shared source removed the live fork's dashboard-set plaintext `ACCESS_TEAM_DOMAIN`/`ACCESS_AUD`/`TENANT_OWNER_EMAIL` → the fork silently dropped to `mode:open-dev` (ISC-30 regressed). RESTORED as **secrets** (extracted `aud`+`iss` from the browser `CF_Authorization` JWT — CF API Access scope was blocked on every available token); secrets survive deploys. **Durable rule: per-fork Access config must be secrets, never dashboard vars.** **Finding 2 — CF error 1042** blocks the deployed worker from fetching pai-tools (both on the same `aitheroad.workers.dev` account/zone). Cross-account recipient forks are unaffected (the actual product target); only the maintainer's co-located fork is blocked. Fix = custom domain for pai-tools + `TOOLS_BASE_URL` (no code change), or a same-account service binding. See ISC-62.
- 2026-06-26 (Block C COMPLETE — ISC-43/44/46, agent=Rex): **The agent control plane's guarded write path + the Assistant pane shipped + verified on the live fork.** **Slice 1 — refined from the planned McpAgent Durable Object to a confirm-gate on the existing stateless `/mcp` handler** (FirstPrinciples: ISC-43/46's verifiable contract is auth→validation→confirm→atomic write+exactly-one-audit-row; interactive elicitation needs BOTH a stateful transport AND an elicitation-capable client — unverifiable here — and a DO `new_sqlite_classes` migration touches the load-bearing deploy-button `wrangler.jsonc`; the confirm-gate is the ISA's own sanctioned fallback at zero deploy-config risk). `add_kb_doc`/`edit_kb_doc` write ONLY on `confirm:true`; `store.ts` commits the doc change + one `mcp_activity` row in a single `env.DB.batch()` (row-count == successful-writes by construction). Migration `0004_mcp.sql`. Verified with a real MCP SDK `Client` (StreamableHTTP) against LIVE D1 via the version preview URL (which bypasses edge Access to reach the bearer-gated /mcp); test data cleaned. **Slice 2** — `/api/assistant` (owner-gated) grounds a prompt in live project/goal/KB context + runs `env.AI` (`@cf/meta/llama-3.1-8b-instruct-fp8`); opt-in Anthropic via AI Gateway (`ANTHROPIC_API_KEY`+`AI_GATEWAY_BASE_URL`, key server-side only); new `/assistant` page + manifest + route. Verified real-Chrome (authed) — correct grounded answer. **Edge Access vs /mcp — RESOLVED (advisor-flagged at the commitment boundary):** the edge Access app 302'd `/mcp` on the protected hostname (a machine agent has the bearer but no Access JWT), so the control plane wasn't agent-reachable in prod — I'd only verified via the version preview URL (which bypasses edge Access). Fixed with a **path-scoped CF Access bypass app** on `my-dashboard.aitheroad.workers.dev/mcp` (app `4dff0061`, decision=bypass): the edge skips Access for `/mcp`, the worker's `verifyBearer` (constant-time, fails closed without `MCP_BEARER`) is the sole gate. Re-verified on the **PROD hostname** (not preview): `/mcp` no-bearer → **401** (worker, not 302 edge), `GET /` → still **302** (main app login intact), full write contract (confirm-gate + atomic audit) passes. `MCP_BEARER` set on Yaron's fork (gitignored `MCP_BEARER.LOCAL.txt`).
- 2026-06-26 (Block C — race-safety reasoning, advisor #2): the existence pre-check in `addKbDoc`/`editKbDoc` is NOT a correctness dependency. `add` is backstopped by the `kb_docs` PRIMARY KEY on slug — a concurrent insert makes the atomic batch fail the UNIQUE constraint → full rollback → no doc, no audit row (caught → isError); the pre-check only yields a friendlier message. `edit`'s pre-check→batch window has no concurrent-delete path (no delete tool exists on the MCP plane), so the doc cannot vanish mid-edit. Net: both write paths are race-safe on this single-owner fork by construction, not by the pre-check. (If a delete tool is added later, move `edit`'s audit insert to a `WHERE EXISTS` conditional in the same batch.)
- 2026-06-26 (Block C — cross-vendor audits, E4): **Forge (GPT-5.5, codex --sandbox read-only) VERDICT = no critical/high/med** — all 6 bypass questions traced SAFE: guard airtight (strict `confirm === true` + non-coercive `z.boolean()`); audit atomicity sound + the TOCTOU closed by the `kb_docs.slug` PRIMARY KEY (concurrent add → PK conflict → full batch rollback → no orphan doc/audit); auth fails closed (`verifyBearer` covers undefined+empty, never echoed); no injection (slug regex authoritative after lowercasing, fully parameterized SQL, XSS-safe `BlockRenderer`+`safeUrl`); assistant owner-only + `mode:access` blocks open-dev + Anthropic key server-side + cost-bounded (4000-char question, 50-row context, max_tokens 1024, 30s timeout). **3 LOW defense-in-depth items FIXED + re-verified live:** (1) 256 KB cap on `blocks` and (2) 200-char title cap enforced in the SERVICE (`assertWritable` — self-defending vs any future caller, not just the MCP zod) — live: >256 KB → isError, normal write → created; (3) assistant prompt delimits dashboard content as untrusted (`<dashboard_context>` + ignore-embedded-instructions). **Cato (E4 mandatory cross-vendor) ran read-only**; its traced checks (`requireViewer` access+owner enforcement, `migrate.ts` glob picking up `0004_mcp.sql`) came back correct. Advisor (commitment boundary) drove the /mcp prod-reachability fix logged above; advisor+Forge converged, no Rule-3 conflict.
- 2026-06-25 (Live Access auth — ISC-30 CLOSED, agent=Rex): **Yaron's own fork is now live, feature-complete, and behind CF Access** at https://my-dashboard.aitheroad.workers.dev. Deployed current code (Blocks A/B/C0) onto the existing `my-dashboard` worker (it was STALE P0 — `/settings` 404'd) and stood up Access. **Key discovery (tested, not assumed — FirstPrinciples): CF Access enforces directly on `*.workers.dev`** (302→`small-fire-f8d3.cloudflareaccess.com`, modern `www-authenticate: Cloudflare-Access` header) → **NO custom domain needed**; the prior belief that Access requires an owned zone was false here. (Aside: `myjarvis.io` is on Erez's CF account `185e19f5…`, not Yaron's `a28d6c97…`, so it was never an option anyway; owned zones = familypilot.app/prijsscherp.nl — moot.) Access app `6e3245b6`, allow-policy email=aitheroad@gmail.com, OTP IdP. **Global-key gotcha:** `~/.config/cloudflare/GLOBAL-API-KEY.txt` has a comment header — extract the credential line (`grep -v '^#' | grep -v '^$' | head -1`); it's a 52-char `cfk_` key on **X-Auth-Email + X-Auth-Key** (Bearer 401s). **Per-fork deploy mechanics:** committed `wrangler.jsonc` keeps IDs omitted (deploy-button + the clean-guard denylists the live D1 id `ee9fee75-f345`); deploy Yaron's instance by patching the vite-generated `build/server/wrangler.json` with the real DB/KV IDs + the 3 Access vars, then `node ./node_modules/wrangler/bin/wrangler.js deploy --config build/server/wrangler.json` (env `CLOUDFLARE_EMAIL`+`CLOUDFLARE_API_KEY`=global key; **`script` pty FAILS in a non-tty shell — run node directly, never bun**). Per-fork values recorded in gitignored `wrangler.deploy.LOCAL.jsonc` (matches `*.LOCAL.*`, not scanned by check:clean). This is effectively the **P4 "first real fork = Yaron" canary** (ISC-50 antecedent) reached early.
- 2026-06-25 (Block C Slice 0): **Phase 3 foundation — shared service layer + read-only MCP control plane (ISC-45, ISC-42).** Extracted all data access into `workers/services/store.ts`; refactored `data.ts` to thin handlers over it; built `workers/mcp.ts` (6 read tools via stateless `createMcpHandler`) mounted at `/mcp` behind the scoped `verifyBearer`. Added deps `agents` + `@modelcontextprotocol/sdk@1.29.0`. **The guarded WRITE path (ISC-43: McpAgent Durable Object + elicitation + `mcp_activity` audit) and the Assistant pane (ISC-44) are DEFERRED to a focused next session** — advisor-confirmed: the DO binding lives in the load-bearing deploy-button `wrangler.jsonc` (migrations/`new_sqlite_classes`), and elicitation can't be exercised without an MCP client, so it doesn't belong under a "green" checkpoint. **Caveat (advisor):** the read MCP transport may change when the McpAgent DO lands (different session model) — the `/mcp` read mount is a working slice, not a frozen contract. Pre-commit advisor gaps closed: no write tool is registered (store has `writeSettings` but MCP exposes only reads); the bearer is never echoed (401 body = `{"error":"unauthorized"}`). **Local MCP verify needs the dev server** (`vite dev`, loads `.dev.vars`) — `vite preview` does NOT load `.dev.vars`, so `MCP_BEARER` is unset there (the page/API verification still uses preview; MCP uses dev).
- 2026-06-25 (Block B): **Phase 2 — Tools embed + Knowledge Base (ISC-38/39/40/41).** Built KB (`kb_docs` table + 3 generic seed docs + `/api/kb` index + `/api/kb/:slug` + a generic XSS-safe 13-type `BlockRenderer`) and Tools (per-fork `tools_key` server-side, redacted from all client responses via `settingsView()`, `/api/tools/*` proxy injecting the key; write-only key field in Settings). tools+kb added to BUILT_PAGES + DEFAULT_ENABLED (6 v1 pages). **Decision — generic renderer, not ported domain blocks:** the source BlockRenderer's "13 types" are MyJarvis pitch-deck/architecture blocks (personal); built a clean generic 13-type doc renderer instead (port the data shape, rebuild the seam — same lineage learning as P1 pages). **Forge cross-vendor audit (GPT-5.5) → `fail` on one HIGH; fixed it + 6 more:** (HIGH) on a bare fork open-dev grants owner to everyone, so an anonymous visitor could spend the stored `tools_key` via `POST /tools/:tool` → now that spend path requires a verified CF **Access** owner (refuses open-dev); live-verified open-dev → 403. (MED) `javascript:`/`data:` href/src XSS in author content → `safeUrl()` allowlist (http/https/mailto/relative), unsafe → inert text; live-verified 0 js: links. (MED) valid-JSON non-array `blocks` crashed `.map` → `BlockRenderer` coerces with `arr()`. (MED) status fetches had no timeout → `AbortSignal.timeout`. (MED) proxy could reflect key if upstream echoed it → redact key from proxied body. (LOW) status validity-probe now owner-only; (LOW) `FALLBACK_PAGES` now includes tools/kb. **Scope note (ISC-38):** the embed = the proxied toolset catalog + connection status (renders the toolset); a FUNCTIONAL tool-invocation panel + the green "valid ✓" badge need a fork with CF Access (ISC-30, deferred) + a live valid pt_ key → [DEFERRED-VERIFY]. The injection mechanism is proven (pai-tools returned "key not recognized", i.e. the key was forwarded).
- 2026-06-25 (Block A): **Phase 1 finished — Settings page + customization (ISC-33/34).** Built owner-only `/settings` (rename/theme/toggle/reorder + export/import), `useRequireEnabled` route-gating, owner gear in Shell, Button/Toggle UI primitives. **Forge cross-vendor audit (GPT-5.5) → `concerns`, 7 verified findings; fixed 5:** (HIGH) import could ship a fork with `home` disabled → enforced `ALWAYS_ON_PAGES=["home"]` in `normalizeConfig` server-side (defense-in-depth, closes ALL write paths, live-verified: import `enabled_pages:["projects"]` → home forced back); (MED) non-owner flashed the owner form during `/api/me` load → gate now waits for BOTH me+settings; (MED) `navFromPages` could emit 404 links for enabled-but-unrouted pages → restricted to `BUILT_PAGES`; (LOW) settings error showed infinite spinner → error checked first; (LOW) added 256 KB import size cap. **2 NOT fixed by design:** open-dev "everyone is owner" on a bare fork = the documented L2 trust model (recipient adds CF Access = ISC-30, deferred per today's scope); client-effect page-gating "flash" = acceptable (page visibility is a UI preference, not an authz boundary — the sensitive surface, settings PUT, is owner-gated server-side).
- 2026-06-25: **Template-cleanliness sweep (clean by construction, not by patching).** Per Yaron — the template must ship with ZERO personal data and need no intervention, not be patched page-by-page. Swept the whole repo: fixed `auth.ts` `DEFAULT_OWNER` (was `aitheroad@gmail.com` → neutral `owner@example.com`; real owner is per-fork `TENANT_OWNER_EMAIL`); genericized provenance comments in `auth.ts`/`db.ts`/`data.ts` (dropped "Yaron's" + the private source-repo name); scrubbed the live D1 uuid + holdings tickers from this ISA. Added `scripts/check-template-clean.mjs` + `bun run check:clean`, wired into `deploy` (fail-closed) — a denylist guard that fails the build if maintainer name/email/addresses/account-IDs/KvK/BTW ever reach the product tree. Verified: guard passes (27 product files), typecheck+build green. **Open call for Yaron:** ISA.md (this builder design doc) still ships in the deploy-button fork and names the maintainer — handling (keep as builder doc / fully genericize / relocate out of the shipped repo) is his decision; guard excludes ISA pending it.
- 2026-06-25: **P1 Slice 1 scope.** P0 closed (ISC-29 deploy-proof verified; ISC-30 CF-Access deferred). This run builds the P1 spine: TanStack Query provider + `app/lib/api.ts` (ISC-23), Zod versioned config (ISC-32), page manifest + sidebar shell (ISC-34 foundation), Hono data handlers + the 4 easy pages with never-blank empty states (ISC-31.1..31.5, 35). Settings export/import UI (ISC-33) + toggle round-trip + migration fixtures (ISC-36/37) follow next slice.
- 2026-06-25: **Portfolio ships EMPTY, not seeded.** The source single-tenant `/api/portfolio` handler embedded the maintainer's real holdings (from a personal portfolio sync). A recipient fork must never carry personal data → portfolio returns an empty productized snapshot + a "connect your portfolio" empty state. Enforces the "wiped of personal data" + isolation principles. (See the 2026-06-25 template-cleanliness sweep + `scripts/check-template-clean.mjs` guard.)
- 2026-06-25: **`getViewer` auth seam (Access-or-open-dev).** Data endpoints can't use bare `requireUser` — on a fork with no CF Access configured (the current live bare-workers.dev fork, and all local dev) it would 401 every page. `getViewer` enforces verified Access when `ACCESS_TEAM_DOMAIN`+`ACCESS_AUD` are set, else grants an owner open-dev viewer. Keeps the gate real where Access exists (ISC-31.5 anti) while letting an unconfigured fork actually show its seeded pages.
- 2026-06-25: **Dev-server hydration quirk DEFERRED (not blocking).** `bun run dev` (react-router dev + `@cloudflare/vite-plugin`) throws `Cannot read properties of null (reading 'useContext')` inside react-router's `Meta`, from `.vite/deps` optimized chunks — DEV ONLY. The PRODUCTION build is clean: `bun run build` green, and the prod preview (`vite preview`, real workerd bundle) renders all 4 pages correctly in real Chrome — and production is exactly what ships to recipients (ISC-29's live fork already rendered prod-built). Root cause is the dev dep-optimizer's module graph (identical render logic works in prod; only the dev bundling differs), not our SSR/CSR output. Tried `resolve.dedupe` + `optimizeDeps.include` for the new react-consuming deps — did not resolve; deeper plugin/version interplay. Follow-up: fix the dev loop (try plugin order / `optimizeDeps.exclude: ['react-router']` / version bump) so local hot-reload works for future slices. Verification this slice ran against the prod preview.
- 2026-06-25: **Advisor (commitment boundary) — proceed.** Advisor flagged "no ISA.md" (false positive — it scans MEMORY/WORK, not this project ISA, which has 53→57 ISCs) and "confirm auth seam not a stub" (already negative-tested: access-configured + no token → 401). Its config-migration + empty-state cautions were already covered by probes. Net: no blocker; the two it would block on are both satisfied. Hydration deferral accepted with the root-cause note above.
- 2026-06-25: **Single-author slice; Forge as independent hardening pass (delegation show-your-math).** This is a small, tightly-coupled vertical slice (shared `api.ts`/config/manifest contracts). A 2nd concurrent write-agent recreates the concurrent-edit conflict class documented for this lineage ([[feedback_concurrent_edit_commit]]); the parallelizable surface is too small to justify it. Delegation = 1 (Forge runs read-then-fix on the quiet committed tree = cross-vendor independence per Yaron's "same-family review shares blind spots" principle), under the E3 soft floor of 2 by design.
- 2026-06-25: **Forge cross-vendor audit (+ independent GPT-5.5 pass) → ONE real defect, FIXED.** Both converged on `workers/lib/config.ts`: zod v4 `.default()` fires only on `undefined`, so a present-but-wrong-type `enabled_pages`/`page_order` THREW (would 500 `/api/settings` on a corrupted/old config) — the exact additive-only invariant my own probe missed (it only tested valid arrays). Fixed by adding `.catch([...])` before `.default([...])` on both arrays + a belt-and-suspenders try/catch in `migrateConfig`. Re-verified: 5/5 malformed inputs now fall back, no throw; typecheck+build green. Auth seam / SQL / migration idempotency / privacy (no personal data) all rated CLEAN by both vendors. This is the value of cross-vendor independence — a same-family check would have shared the blind spot.
- 2026-06-24: Repo home = `~/Projects/my-dashboard-app` (fresh dir + fresh git), per Yaron — leaves the set-aside `~/Projects/my-dashboard` clone-fork untouched as reference. GitHub repo name TBD at the deploy-button step.
- 2026-06-24: Verified the actual template before writing this ISA (cloned `cloudflare/react-router-hono-fullstack-template`, inspected real structure) rather than inferring — Explore's structural details came from the yaron repo, not the template. Real facts: Hono 4.8.2 / RR 7.6.3 / vite 6 / wrangler.jsonc / `workers/app.ts` Hono catch-all / `cloudflare.publish:true` block powers the Deploy button / template's stated UI system is shadcn/ui.
- 2026-06-24: Server dir = `workers/` (template), so port target is `workers/lib/*`, correcting the plan's `/server/lib/*`.
- 2026-06-24: Custom idempotent migration runner is authoritative (boot-guard + `bun run migrate`), not wrangler-native `d1 migrations` — sidesteps the JSONC/TOML remote-migration trap and gives us the single-flight + `_migrations` tracking the plan specifies.
- 2026-06-24: shadcn-admin shell + page components DEFERRED to P1 — not required for the P0 isolation milestone (plan: "prove the milestone BEFORE porting any page").
- 2026-06-24: Deploy via node (`/opt/homebrew/bin/node`) wrapped in `script -q /dev/null`, never bun — bun hangs `wrangler` uploads on this Mac ([[reference_wrangler_via_bun]]). New build is Workers → `wrangler deploy` (not `pages deploy`).
- 2026-06-24: **Toolchain bump required for omitted-IDs.** The template pins wrangler 4.21.2 + vite-plugin 1.7.5, both of which REJECT `wrangler.jsonc` bindings without `database_id`/`id`. Bumped to `wrangler ^4.104.0` + `@cloudflare/vite-plugin@1.42.2` (uses the hoisted wrangler) — these tolerate omitted IDs, which is the whole deploy-button premise. Pinned in package.json + bun.lock so the button builds with matching versions.
- 2026-06-24: **Migration concurrency redesign (advisor-driven).** Forge's first runner claimed the `_migrations` row BEFORE running DDL → a concurrent loser could read the claim and serve a half-migrated DB (TOCTOU). Replaced with an atomic-batch completion barrier: DDL + completion row commit together in one `D1.batch()`; the loser hits a PK conflict → full rollback → re-reads → proceeds only after the winner committed. Row presence now means "fully applied," never "claimed."
- 2026-06-24: **AI binding triggers a remote connection in `bun run dev`** ("⎔ Establishing remote connection…") since Workers AI can't run locally — resolves on its own here; note it for fork dev onboarding.
- 2026-06-24: `/api/health` returns a binding-presence map (advisor hardening) so a partially-provisioned fork is diagnosable rather than a blanket 500.
- 2026-06-24: ISC-23 (TanStack provider + api.ts) moved to P1 — the P0 milestone is isolation/migrations, and no page consumes the data layer yet; frontend stays the template placeholder.
- 2026-06-30: **Tools workspace LIVE-VERIFIED end-to-end** (agent=Ringo, Interceptor authed Chrome). TTS, Speak→Text (whisper), Read Text (OCR), and Gallery all exercised with real inputs against the live fork — every native `env.AI` call returned 200, NO 1042. Closed ISC-56/57/58/59/63/64/65/66 → progress 66/77. File injected into the hidden `<input type=file>` via DataTransfer (fires React onChange) since the eval transport caps ~6KB — large base64 streamed into the page in ~4.5KB chunks, then injected.
- 2026-06-30: **OCR MODEL FINDING (golden-image gap, decision OPEN).** `llava-1.5-7b` OCR fidelity is WEAK: input "Invoice 4071 EUR 128.50" → returned "4007171 EURO" (garbled digits, dropped words). The path + render are correct, but the extraction quality is below Yaron's "really tight" bar. Whisper (`whisper-large-v3-turbo`) by contrast is strong. **Options for the golden image:** (a) swap OCR to a stronger vision model — Mistral Small 3.1 (EU-safe, already noted in code comment) or Llama-3.2-vision (needs Meta model agreement on the account); (b) keep llava + lower the page's promise ("rough text extraction"); (c) route OCR through a dedicated OCR path. **RESOLVED 2026-06-30:** chose (a) — swapped to **Mistral Small 3.1** (`@cf/mistralai/mistral-small-3.1-24b-instruct`), verified live on the account via REST first (exact extraction) then live on staging through the UI. EU-safe, no Meta agreement, best fidelity. ISC-58 closed.
- 2026-06-30: **workerd outbound-WebSocket scheme gotcha (REUSABLE).** The Edge TTS module first 502'd live ("failed unexpectedly" = a raw non-ToolError). Cause: `fetch()` was called with a `wss://` URL — workerd's fetch-upgrade WebSocket **requires the `https://` scheme** (it rejects `wss://`). Fix = one-token change `wss://`→`https://` in `edge-tts.ts`; Hebrew TTS went green immediately. Also added explicit `Edge connect failed: …` / `Edge handshake rejected (HTTP n)` ToolErrors so a future failure surfaces its cause instead of the generic wrapper. Mirrors how the protocol was de-risked: prove in bun, port to workerd, live-probe the runtime difference.
- 2026-06-30: **Hebrew TTS confirmed dashboard-NOT-capable without the OpenAI key.** Live test: Hebrew text → engine stayed `deepgram:aura-1` (English), silent fallback, no error. So Yaron's "Hebrew works pretty well" is pai-meet / Family OS, not this dashboard. OpenAI key deferred by Yaron (2026-06-30) — Hebrew TTS on the dashboard stays unavailable until a key is set; the banner is honest about it.
- 2026-06-30: **ISC-61/67 (auth refusal) status.** Edge CF Access returns 302 for ALL unauthenticated requests (verified live). The app-level non-owner/open-dev 403 branch is code-present + Forge-audited but cannot be separately probed on an Access-configured fork (would need a second, non-owner identity) → live probe deferred to the first real recipient fork (fork-2 / Noa). Left `- [ ]`.

## Changelog

- conjectured: a fresh Workers skeleton + proven modules ported from `my-jarvis-dashboard-yaron` is faster and safer than green-fielding or cleaning the old clone-fork. | refuted_by: (open) | learned: (pending P0 completion) | criterion_now: ISC-11 (db.ts ports unchanged) is the load-bearing test of the "port the proven" conjecture.
- conjectured: the Deploy-to-Cloudflare button auto-provisions an isolated D1+R2+KV per click when `wrangler.jsonc` omits resource IDs. | refuted_by: (open — proven at ISC-29) | learned: omitted-IDs requires wrangler ≥4.40-ish (4.104 confirmed) + a matching vite-plugin; the template's pinned 4.21/1.7 reject it. | criterion_now: ISC-29 is the cheapest test of the riskiest, most load-bearing assumption in the whole project.
- conjectured: an `INSERT OR IGNORE` PK-claim row is a sufficient single-flight lock for boot-time migrations. | refuted_by: advisor review 2026-06-24 — a claim is not a completion barrier; a concurrent loser reads the claimed row and serves against a half-migrated DB (TOCTOU). | learned: under D1's no-long-transaction model the only safe barrier is committing the migration body AND its completion marker in ONE atomic `D1.batch()`, so row-presence ⟺ fully-applied. | criterion_now: ISC-28 reworded to "guaranteed by construction (atomic batch + PK rollback)".
- conjectured: deploying current source to the live fork is a safe code-only update. | refuted_by: live `/api/me` 2026-06-29 returned `mode:open-dev` post-deploy — `wrangler deploy` wiped the dashboard-set plaintext Access vars, regressing ISC-30. | learned: `wrangler deploy` syncs vars from config and drops dashboard-only plaintext vars; **secrets persist**. Per-fork Access config (TEAM_DOMAIN/AUD/OWNER) must be set as secrets. | criterion_now: ISC-30 re-verified after restoring the three as secrets (`mode:access`, isOwner:true).
- conjectured: the server-side `/api/tools/*` proxy that worked in local dev would also reach pai-tools from the deployed fork. | refuted_by: live test 2026-06-29 — CF **error 1042** blocks same-account worker→worker subrequests (my-dashboard + pai-tools both on `aitheroad.workers.dev`); flux/status/gallery all 1042. | learned: same-account workers.dev→workers.dev is blocked; cross-account is allowed → recipient forks work, but the maintainer's co-located fork needs pai-tools on a custom domain (set `TOOLS_BASE_URL`, no code change) or a service binding. | criterion_now: ISC-62 reworded to BLOCKED-by-1042 with the custom-domain fix as the unblock path. | **RESOLVED 2026-06-29:** rather than route around 1042 (custom domain / service binding), the proxy was deleted — the tools run natively on the fork's own `env.AI`, so there is no worker→worker subrequest to block. 1042 is gone by construction; ISC-62 unblocked. The deeper learning: the proxy existed only to reach a separate Worker; once the caller is already authenticated to THIS fork, the second Worker (and its key) was never a hard constraint, just inherited form.
- conjectured: giving every Zod field `.default()` makes the config parse throw-proof, so an old/garbage stored config always migrates instead of crashing. | refuted_by: Forge + independent GPT-5.5 audit 2026-06-25 — in zod v4 `.default()` engages ONLY on `undefined`; a present-but-wrong-type value throws ZodError, which would 500 `/api/settings`. My own probe missed it (tested only valid arrays). | learned: in zod v4, robustness against malformed input requires `.catch()` (not `.default()`) on every field that can hold a wrong type; `.default()` covers absence, `.catch()` covers corruption. | criterion_now: ISC-37 (additive/garbage config never breaks) now backed by `.catch()` on the array fields + a migrateConfig try/catch; verified 5/5 malformed inputs fall back.
- conjectured: the Microsoft Edge read-aloud TTS protocol (proven from-scratch in a bun probe) would port to a Cloudflare Worker unchanged. | refuted_by: live 502 on staging — workerd's fetch-upgrade WebSocket REJECTS the `wss://` scheme that bun's `new WebSocket` accepts. | learned: outbound WS in workerd = `fetch(httpsUrl,{headers:{Upgrade:"websocket"}})` → `resp.webSocket` → `.accept()`; the URL MUST be `https://` not `wss://`. Everything else (GEC SHA-256 via crypto.subtle, SSML frames, binary frame parse) ports verbatim. | criterion_now: ISC-73 (Edge Hebrew TTS verified live on workerd, staging v7999c261).

## Verification

- conjectured: P1 pages "port" from `my-jarvis-dashboard-yaron` near-verbatim (per the plan's "port existing page components + their /api/* handlers"). | refuted_by: the source is Pages-Functions + react-admin/atomic-crm; the target is Hono + RR7 + bare TanStack Query — the API handlers are rewritten as Hono routes and the pages are rebuilt on the new data layer; and the source `/api/portfolio` embeds Yaron's real holdings. | learned: "port the proven" holds for the DATA layer (`getDb` + SQL shapes port unchanged) but NOT the framework seam (handlers + UI rebuilt) — port the data, rebuild the seam; and a "port" can smuggle personal data, so privacy is an explicit anti-criterion. | criterion_now: ISC-31.1–31.4 verify rebuilt-on-stack; ISC-31.3 ensures the empty productized portfolio.
- conjectured: a green local `bun run build` + a green dev server together gate "the app renders." | refuted_by: `bun run dev` (cloudflare-vite-plugin) crashes in react-router's `Meta` while the production build renders all pages cleanly in real Chrome. | learned: for this stack, the PRODUCTION preview (`vite preview`, real workerd) is the faithful render check — verify there, not against the dev server; dev hot-reload is a separate, deferrable concern. | criterion_now: the P1 UI verification evidence is captured against the prod preview.

P0 buildable surface (run 2026-06-24, E3):
- ISC-2: `git log --oneline` → single commit `6759d59`; `git remote -v` empty.
- ISC-4: `bun install` → "226 packages installed"; `bun.lock` present, `package-lock.json` absent.
- ISC-5–10: `grep wrangler.jsonc` → DB/BUCKET/KV/AI bindings + `nodejs_compat`; "NO committed IDs ✓".
- ISC-11–14: `Read workers/lib/{db,auth}.ts` → `getDb` verbatim; `jose.jwtVerify` checks aud+iss+sig+exp; `verifyBearer` present.
- ISC-16,18: `bun run migrate` ×2 → "Applied: 0001_init.sql / 0 pending", then "Applied: none / 0 pending".
- ISC-17,19: wiped `.wrangler`; fresh-DB `/api/me` → **401 (not 500)**; `SELECT … sqlite_master` → `_migrations` + `settings` present.
- ISC-20,21,22: `curl` → `/api/health` 200 `{ok:true,bindings:{db,bucket,kv,ai all true}}`; `/api/me` 401; `/` 200 rendering "My Dashboard".
- ISC-24,25: `bun run typecheck` clean; `bun run build` → "✓ built", worker + client assets emitted (migration `.sql` bundled).
- ISC-27: health 200 public + me 401 → auth gate enforced, health is the sole public route.
- ISC-28: code — atomic `D1.batch([...ddl, markComplete])` in `workers/lib/migrate.ts` (completion barrier by construction).
- ISC-1,29,30: ISC-1/29 closed (deploy-proof, prior run); ISC-30 still DEFERRED (CF Access on the fork).

P1 Slice 1 (run 2026-06-25, E3) — commit `75769cc`:
- Build: `bun run typecheck` EXIT 0 (after `wrangler types` + `react-router typegen` + `tsc -b`); `bun run build` EXIT 0 (client + SSR bundles, 15.6 kB CSS = Tailwind compiled the new classes).
- Migrate: `bun run migrate` applied `0002_p1.sql`; 2nd run "Applied: none / 0 pending" (idempotent).
- API (local dev, open-dev viewer): `/api/health` 200 all-bindings-true; `/api/me` 200 `{owner@local,open-dev}`; `/api/settings` 200 fully-default config + `pages:[home,projects,goals,portfolio]`; `/api/projects` 200 (2 demo, goal_count 1/2); `/api/goals` 200 (3, project_name joined); `/api/portfolio` 200 `{configured:false, holdings:[]}`.
- Settings write: `PUT /api/settings` dropping "goals" + rename + theme:dark → `pages:[home,projects,portfolio]`, display_name "Rex Test", theme dark; re-GET reflects; restore → 4 pages.
- Auth seam probe: access-env (`ACCESS_TEAM_DOMAIN`+`ACCESS_AUD`) + no token → `getViewer` null (= 401); bare env → open-dev owner.
- Config probe: `migrateConfig({})` defaults; `migrateConfig({schemaVersion:0,enabled_pages:[...,"FUTURE_PAGE"],theme:"dark"})` → v1, theme kept, FUTURE_PAGE dropped; `migrateConfig(null|"str"|{theme:99})` never throws.
- UI (real Chrome, Interceptor, against prod preview `vite preview` @ :4173): Home (nav + 2/2 stat cards + welcome + recent projects), `/projects` (2 cards), `/goals` (3 w/ project labels), `/portfolio` ("No portfolio connected" empty state). Dev server (`react-router dev`) has a separate cloudflare-vite-plugin hydration error — prod verified instead (see Decisions).

Block A — Settings + customization (run 2026-06-25, E4) — ISC-33, ISC-34:
- Render (real Chrome, prod preview): `/settings` a11y tree = 4 toggles (Home `disabled` always-on), theme `<select>`, display-name input, file input, 8 reorder arrows (first ↑ + last ↓ disabled), owner Settings gear in sidebar (`/settings` link present).
- ISC-34 toggle: DOM-click "Toggle Goals" → re-render `aria-checked=false` → click Save → `GET /api/settings` `pages:[home,projects,portfolio]` (goals dropped, persisted).
- ISC-34 sidebar: reload `/` → sidebar links = `/,/projects,/portfolio,/settings` (no Goals).
- ISC-34 route-gating: open `/goals` (disabled) → `location.pathname="/"`, h1 "My Dashboard" (useRequireEnabled redirect).
- ISC-34 reorder: `PUT page_order:[home,portfolio,projects,...]` → `pages:[home,portfolio,projects]`.
- ISC-33 import-migrate: `PUT {schemaVersion:0,theme:"dark",display_name:"Imported Fork"}` → config schemaVersion 1, theme dark, name applied.
- ISC-33 robustness: `PUT {enabled_pages:"not-an-array",theme:99,page_order:{}}` → HTTP 200 (no throw).
- Forge HIGH fix (live): `PUT {enabled_pages:["projects"]}` (no home) → `enabled_pages:["projects","home"]` (always-on home forced back by normalizeConfig).
- Build: typecheck EXIT 0, build EXIT 0, check:clean 28 files 0 hits.

Block B — Tools + Knowledge Base (run 2026-06-25, E4) — ISC-38,39,40,41:
- Migrate: `bun run migrate` applied `0003_p2.sql`; `/api/kb` → 3 docs [customize, blocks-reference, welcome].
- ISC-40: `/api/kb/blocks-reference` → 14 blocks, 13 DISTINCT types (callout,code,divider,heading,hero,image,keyvalue,links,list,paragraph,quote,steps,table). Real-Chrome detail render: h1 "Block reference", 1 h2, 4 ul/ol, 1 table, 1 pre, 1 blockquote, 1 hr, 1 figure, 2 external links — all render, no failure.
- 6-page default: `resolvePages(migrateConfig({schemaVersion:1}))` = [home,projects,goals,portfolio,tools,kb]; sidebar real-Chrome shows all 6 + Settings.
- ISC-38: `/tools` no key → "Tools not configured" + Open Settings link. Key set → "Available tools" cards flux/tts/ocr/whisper (proxied catalog) + banner.
- ISC-39: `PUT {tools_key}` → response `tools_key:null, tools_configured:true` (redacted); `GET /settings` → `tools_key:null`; `grep build/client` for the key VALUE → ABSENT. Proxy injection PROVEN: pai-tools replied "key not recognized" (401) = the key was forwarded server-side (test key revoked → green-badge deferred).
- Build: typecheck EXIT 0, build EXIT 0, check:clean 33 files 0 hits.
- Forge audit fixes (post-commit re-verify): `POST /api/tools/flux` in open-dev → HTTP 403 "Enable Cloudflare Access" (no anonymous key-spend); `/kb-doc/blocks-reference` re-render = all 13 types, 0 `javascript:` links.

Block C Slice 0 — service layer + read MCP (run 2026-06-25, E4) — ISC-42, ISC-45:
- ISC-45: `workers/services/store.ts` is the single data home; `data.ts` refactored to thin handlers over it. HTTP regression (dev): /api/projects 2, /api/goals 3, /api/kb 3, /api/settings tools_key=null, /api/me owner — all 200.
- ISC-42 bearer gate: `POST /mcp` no bearer → 401; wrong bearer → 401; valid bearer → initialize 200 (`mcp-session-id` exposed).
- ISC-42 tools: `tools/list` → list_projects, list_goals, get_portfolio, list_kb, get_kb_doc, get_settings (6 read tools); `tools/call list_projects` → live data ("Launch my dashboard", "Build my personal OS").
- Security: 0 write tools registered; 401 body never echoes the token (`grep leakcheck-SECRET` → 0).
- Verified on the DEV server (`vite dev` loads `.dev.vars`); `vite preview` does not load `.dev.vars`.
- Build: typecheck EXIT 0, build EXIT 0 (worker bundle 1.76 MB with MCP SDK — within Workers limits; watch on deploy).

Live Access auth (run 2026-06-25, E3) — ISC-30:
- Access enforcement: unauth `curl` of `/` AND `/api/health` → HTTP 302 → `small-fire-f8d3.cloudflareaccess.com/cdn-cgi/access/login/...` (workers.dev protected directly; `www-authenticate: Cloudflare-Access` present).
- Authed (real Chrome, existing org SSO session — no fresh OTP): `net log` shows `/api/me` → 200 body `{"email":"aitheroad@gmail.com","isOwner":true,"mode":"access"}` (mode:access ⇒ verified CF-Access JWT, NOT open-dev fallback).
- Current code live (was stale P0 `/settings` 404): sidebar = Home/Projects/Goals/Portfolio/Tools/Knowledge Base/Settings; Home renders seeded "2 active projects / 2 active goals" + recent projects; `/settings` owner page renders (display-name/theme/page-toggle/reorder/Tools connection).
- Migrations self-healed via boot-guard on first authed `/api/*`: remote D1 `_migrations` now [0001_init, 0002_p1, 0003_p2]; `kb_docs` count = 3.
- Deploy: `node wrangler deploy --config build/server/wrangler.json` (global key via CLOUDFLARE_EMAIL+CLOUDFLARE_API_KEY) → Version `22cae6ad`; binding table confirmed KV/DB/BUCKET/AI + ACCESS_TEAM_DOMAIN/ACCESS_AUD/TENANT_OWNER_EMAIL.
- NEGATIVE tests (advisor-driven; via the version PREVIEW URL `22cae6ad-my-dashboard.aitheroad.workers.dev`, which bypasses the edge Access app → hits the worker's own JWT check directly): `/api/health`→200; `/api/me` no-header→401; `/api/me` forged `Cf-Access-Jwt-Assertion`→401; `/api/projects` no-header→401; spoofable `Cf-Access-Authenticated-User-Email` header alone→401. Proves: fail-closed (open-dev NOT reached when vars set), real JWT verification (not header-presence), all data routes gated, spoofable header untrusted.
- **Forge cross-vendor audit (GPT-5.5, codex --sandbox read-only) → VERDICT = pass, NO bypass.** Independently confirmed (file:line): jose v6 enforces exp/nbf by default; identity only from verified `payload.email`; spoofable header never read (grep); every `/api/*` gated via getViewer/requireViewer, `/api/health` sole public; open-dev unreachable in prod (env vars can't be request-spoofed); non-allowlisted verified email → 401 independent of edge policy; AUD+issuer pinned per-fork. 3 LOW/info nits (NOT bypasses): `verifyBearer` early length-check leaks token length via timing (unexploitable; high-entropy secret); `/mcp` reachable on a bare fork if `MCP_BEARER` set (by design, separate control plane, fails closed without secret); `requireUser` is exported-but-unused dead code. All three are naturally in scope for Block C Slice 1 (the MCP write seam). Advisor (concerns) + Forge (pass) converge; no Rule-3 conflict. Audit artifact: `MEMORY/WORK/20260625-100000_my-dashboard-auth-audit/forge-audit-final.txt`.
- OPEN hardening (non-blocking, Yaron's call): worker has `previews_enabled:true` → every version's preview URL bypasses the edge Access app (data still fails closed; only the SSR page shell renders). Disable via the subdomain API to remove the bypass, OR keep for deploy-verification. Also: make open-dev gate on an explicit dev flag rather than "Access vars absent" so a recipient who forgets to configure Access isn't silently wide-open (template hardening for P4 forks).
